{
  "name": "qr-data-transfer-system",
  "version": "1.1.0",
  "description": "QR Data Transfer System - Secure cross-network data transfer via QR codes",
  "author": "TieLi",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/ttieli/Scan.git"
  },
  "homepage": "https://ttieli.github.io/Scan/",
  "keywords": [
    "qr-code",
    "data-transfer",
    "pwa",
    "offline-first",
    "cross-network"
  ],
  "scripts": {
    "version:check": "node scripts/check-version.js",
    "version:patch": "node scripts/release.js patch",
    "version:minor": "node scripts/release.js minor",
    "version:major": "node scripts/release.js major",
    "release": "node scripts/release.js",
    "icons:generate": "node scripts/generate-icons.js",
    "serve": "npx http-server -p 8888",
    "serve:https": "npx http-server -p 8443 -S -C cert.pem"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "sharp": "^0.34.3"
  },
  "engines": {
    "node": ">=14.0.0",
    "npm": ">=6.0.0"
  }
}
